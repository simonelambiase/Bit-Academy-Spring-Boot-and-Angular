import { IsFullNameValidDirective } from './is-full-name-valid.directive';

describe('IsFullNameValidDirective', () => {
  it('should create an instance', () => {
    const directive = new IsFullNameValidDirective();
    expect(directive).toBeTruthy();
  });
});
